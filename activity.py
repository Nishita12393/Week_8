import numpy as np
integers = np.arange(10)
print(integers)
print(f"Shape of the array is {integers.shape}and the data type of the array is {integers.dtype}")
multiply = integers * 2
print (f"Each element after multiplying by 2: {multiply}")
